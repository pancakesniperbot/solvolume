[build]
command = "cd client && npm install && npm run build"

[build.environment]
NODE_VERSION = "20"

[site]
bucket = "./client/dist"

[pages]
pages_build_output_dir = "client/dist"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200 